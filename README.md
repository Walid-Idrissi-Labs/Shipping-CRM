# Shipping CRM

A multi-tenant logistics & shipping CRM that connects **shipping providers (prestataires)** with their **commercial clients** and public visitors (tracking, quotes, account requests).

Built with **Laravel 12** for the API/backend and **React 18 + Vite 8** for the SPA frontend.